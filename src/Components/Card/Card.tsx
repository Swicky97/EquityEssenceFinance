import React from 'react'

interface Props {
    companyName: string;
    ticker: string;
    price: number;
}

const Card: React.FC<Props> = (props : Props) => {
  return (
    <div>
      
    </div>
  )
}

export default Card
